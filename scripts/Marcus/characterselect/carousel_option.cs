using System.Collections.Generic;
using UnityEngine;

public class carousel_option : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Animator anim;
    public character_class_data class_data;
    public int option_index;
    //may add other stat diffs/starting passives later  
    //maybe ill add ui to display what hat they have.

}
